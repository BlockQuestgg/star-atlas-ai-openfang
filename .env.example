# LLM Provider — set one of: anthropic | openai | openrouter
# Provider presets live in config/providers/*.toml
LLM_PROVIDER=anthropic

# API keys — only the key matching your LLM_PROVIDER is required
ANTHROPIC_API_KEY=sk-ant-...
# OPENAI_API_KEY=vibeproxy
# OPENROUTER_API_KEY=sk-or-...

# Community vault path (absolute path on host) — optional, mounted read-only
# COMMUNITY_VAULT_PATH=/path/to/community-vault

# sa-kb-mcp — community vault path for the MCP server / CLI
# Falls back to ../../vaults/community relative to the binary if unset
# SA_COMMUNITY_VAULT_PATH=/path/to/vaults/community

# Discord bot token — optional, commented out by default
# Setup: https://discord.com/developers/applications
#   1. Create application → add bot
#   2. Enable "Message Content Intent" under Privileged Gateway Intents
#   3. OAuth2 scopes: bot | Permissions: Send Messages, Read Message History
#   4. Copy bot token here
# DISCORD_BOT_TOKEN=
